export const metadata = {
  title: 'Mac Setup',
  description: "Apps I use, CLI tools & more",
  tags: 'Easy Access, Self Management',
}

# {metadata.title}

{metadata.description}

---

### Apps (App Store)

- Hand Mirror
- Lockera Widgets
- MenubarX

### Apps (Homebrew Casks)

- Google Chrome - `brew install --cask google-chrome`
- Rectangle - `brew install --cask rectangle`
- Visual Studio Code - `brew install --cask visual-studio-code`

### CLI Installs

- Bun - `curl -fsSL https://bun.sh/install | bash`
- Homebrew - `/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"`
- Homebrew Installs - `brew install gh git nvm rsync zsh-autosuggestions zsh-history-substring-search zsh-syntax-highlighting`
- Terminal - `bunx zshify`
