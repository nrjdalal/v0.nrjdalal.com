export const metadata = {
  title: 'Mac Setup',
  description: "Apps I use, CLI tools & more",
  tags: 'Easy Access, Self Management',
}

# {metadata.title}

{metadata.description}

---

### Initial

- git
  ```sh
  git config --global user.name "Neeraj Dalal"
  git config --global user.email "admin@nrjdalal.com"
  git config --global init.defaultBranch "main"
  ```

### Apps (App Store)

- Hand Mirror
- Lockera Widgets
- MenubarX

### Apps (Homebrew Casks)

- Google Chrome
  ```sh
  brew install --cask google-chrome
  ```
- Rectangle
  ```sh
  brew install --cask rectangle
  ```
- Visual Studio Code
  ```sh
  brew install --cask visual-studio-code
  ```

### CLI Installs

- Bun
  ```sh
  curl -fsSL https://bun.sh/install | bash
  ```
- Homebrew
  ```sh
  /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
  ```
- Homebrew Installs
  ```sh
  brew install gh git nvm rsync zsh-autosuggestions zsh-history-substring-search zsh-syntax-highlighting
  ```
- Terminal
  ```sh
  bunx zshify
  ```
